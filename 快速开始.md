# 🚀 Lumen Gallery - 快速开始指南

欢迎使用 Lumen Gallery！这份指南将带你从零开始创建自己的照片画廊。

## 📋 前置条件

- ✅ Node.js 18+ 已安装
- ✅ pnpm 已安装（或使用 npm/yarn）
- ✅ 已运行 `pnpm install` 安装依赖

## 🎯 第一步：了解项目结构

```
Lumen-Gallery/
├── public/
│   ├── photos/          # 📸 你的原始照片放这里！
│   ├── processed/       # 🔄 处理后的缩略图（自动生成）
│   └── photos.json      # 📊 图片元数据（自动生成）
├── src/
│   ├── pages/           # 📄 Astro 页面（文件即路由）
│   ├── components/      # 🧩 可复用组件
│   ├── lib/
│   │   ├── storage/     # 💾 存储接口（本地/R2/GitHub）
│   │   └── image/       # 🖼️  图片处理工具
│   └── layouts/         # 🎨 页面布局
├── scripts/
│   └── build-images.js  # ⚙️  图片处理脚本
└── .env                 # 🔐 环境变量配置
```

## 🖼️ 第二步：添加你的照片

### 方式 1：本地存储（推荐新手）

1. **将照片放入 `public/photos/` 目录**

```bash
public/photos/
  ├── vacation/
  │   ├── beach.jpg
  │   └── sunset.jpg
  └── family/
      └── birthday.jpg
```

2. **支持的格式**：

   - 常见格式：JPG, PNG, GIF, WebP
   - RAW 格式：HEIC, HEIF, TIFF（会自动转换）

3. **目录结构即标签**：
   - `photos/travel/japan/tokyo.jpg` → 标签：`travel`, `japan`

### 方式 2：Cloudflare R2（适合大量照片）

1. 在 `.env` 中配置：

```env
STORAGE_MODE=r2
R2_ACCOUNT_ID=your_account_id
R2_ACCESS_KEY_ID=your_access_key
R2_SECRET_ACCESS_KEY=your_secret_key
R2_BUCKET_NAME=your_bucket_name
```

### 方式 3：GitHub 仓库

1. 在 `.env` 中配置：

```env
STORAGE_MODE=github
GITHUB_OWNER=your-username
GITHUB_REPO=your-repo
GITHUB_BRANCH=main
GITHUB_PATH=images
```

## ⚙️ 第三步：处理图片

运行图片处理脚本：

```bash
pnpm process-images
```

**这个脚本会自动：**

- ✅ 扫描所有照片
- ✅ 提取 EXIF 信息（相机型号、GPS、拍摄参数）
- ✅ 生成 3 种尺寸缩略图（300px, 800px, 1600px）
- ✅ 创建 Blurhash 占位符（加载动画）
- ✅ 转换 HEIC/TIFF 等格式为 JPEG
- ✅ 生成 `photos.json` 元数据文件

**输出示例：**

```
🖼️  Starting image processing...

Found 3 images

Processing: vacation/beach.jpg
  Format: jpg
  Dimensions: 4032x3024
  Extracting EXIF...
  Generating blurhash...
  Generating thumbnails...
  Saved: beach_300w.jpg (45 KB)
  Saved: beach_800w.jpg (180 KB)
  Saved: beach_1600w.jpg (520 KB)
✓ Completed: vacation/beach.jpg

✅ Processed 3/3 images
💾 Metadata saved to: public/photos.json
```

## 🚀 第四步：启动开发服务器

```bash
pnpm dev
```

访问 http://localhost:4321/ 查看你的画廊！

## 📝 第五步：自定义你的网站

### 修改网站信息

编辑 `src/consts.ts`：

```typescript
export const SITE_TITLE = "我的照片画廊"
export const SITE_DESCRIPTION = "记录生活的美好瞬间"
```

### 创建首页画廊

编辑 `src/pages/index.astro`：

```astro
---
import Layout from '../layouts/BlogPost.astro';

// 读取处理好的图片元数据
const photosData = await fetch('/photos.json').then(r => r.json());
---

<Layout title="我的照片">
  <div class="photo-grid">
    {photosData.map(photo => (
      <div class="photo-card">
        <img
          src={photo.thumbnails.medium.url}
          alt={photo.filename}
          loading="lazy"
        />
        <p>{photo.exif.make} {photo.exif.model}</p>
      </div>
    ))}
  </div>
</Layout>

<style>
  .photo-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));
    gap: 1rem;
  }
</style>
```

## 🎨 第六步：学习 Astro（对比 Vue3）

### 关键差异

| 特性     | Vue3                            | Astro                                          |
| -------- | ------------------------------- | ---------------------------------------------- |
| 组件语法 | `<template>` + `<script setup>` | `---` (frontmatter) + HTML                     |
| 响应式   | `ref()`, `reactive()`           | 构建时静态，需要交互用 `client:*`              |
| 路由     | Vue Router 配置                 | 文件系统路由（`pages/about.astro` = `/about`） |
| 数据获取 | `onMounted` + `fetch`           | 直接在 frontmatter 中 `await`                  |

### 示例：显示单张照片

**Vue3 方式：**

```vue
<script setup>
import { ref, onMounted } from "vue"

const photo = ref(null)

onMounted(async () => {
  const res = await fetch("/api/photo/1")
  photo.value = await res.json()
})
</script>

<template>
  <img v-if="photo" :src="photo.url" />
</template>
```

**Astro 方式：**

```astro
---
// 构建时直接获取数据！
const photos = await import('../../public/photos.json');
const photo = photos[0];
---

<img src={photo.thumbnails.medium.url} alt={photo.filename} />
```

## 📚 常用命令速查

```bash
# 开发
pnpm dev                    # 启动开发服务器
pnpm process-images         # 处理图片

# 构建
pnpm build:full             # 处理图片 + 构建网站
pnpm build                  # 仅构建网站（假设图片已处理）
pnpm preview                # 预览构建结果

# 部署
# 将 dist/ 目录上传到 Vercel/Netlify/GitHub Pages
```

## 🔧 常见问题

### Q1: 图片没有显示？

**A**: 检查步骤：

1. 确认图片在 `public/photos/` 目录
2. 运行 `pnpm process-images`
3. 检查 `public/photos.json` 是否生成
4. 重启开发服务器 (`pnpm dev`)

### Q2: HEIC 格式不支持？

**A**: HEIC 会自动转换为 JPEG，确保：

1. 依赖已安装：`pnpm install`
2. 运行 `pnpm process-images`

### Q3: 如何添加水印？

**A**: 修改 `src/lib/image/processor.ts`，在 `generateThumbnail` 中使用 sharp 的 `composite` 方法。

### Q4: 如何自定义缩略图尺寸？

**A**: 编辑 `scripts/build-images.js`：

```javascript
const metadata = await processImages(storage, outputDir, {
  thumbnailSizes: [400, 1000, 2000], // 自定义尺寸
  quality: 90, // 自定义质量
})
```

## 🎯 下一步

✅ **完成基础设置后，可以：**

1. 📖 阅读 `Astro学习笔记.md` 深入学习 Astro
2. 🎨 实现瀑布流布局（参考开发计划.md）
3. 🗺️ 添加地图功能（基于 GPS 数据）
4. 🔍 实现标签筛选和搜索
5. 📱 优化移动端体验

## 📖 相关文档

- [Astro 官方文档](https://docs.astro.build/)
- [Sharp 文档](https://sharp.pixelplumbing.com/)
- [EXIFR 文档](https://github.com/MikeKovarik/exifr)
- [Blurhash 文档](https://blurha.sh/)

## 💡 提示

- 每次添加新照片后，记得运行 `pnpm process-images`
- 开发时可以只放少量照片测试，避免处理时间过长
- 图片处理结果会缓存在 `public/processed/`，可手动删除重新生成

---

**祝你使用愉快！有问题随时提问 🚀**
