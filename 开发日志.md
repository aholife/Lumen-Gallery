# 📅 Lumen Gallery 开发日志

本文档用于记录 Lumen Gallery 项目的开发进度、变更历史以及后续开发计划。Agent 在进行开发前请务必阅读此文档，以确保上下文一致性。

## 📊 项目概况

- **项目名称**: Lumen Gallery
- **核心技术**: Astro, TypeScript, Tailwind CSS, React (用于复杂交互组件)
- **当前阶段**: 基础设施与核心图片处理流程已完成，正在进行前端展示组件开发。

## ✅ 已完成功能 (截至 2026-02-10)

### 基础设施
- [x] 项目初始化 (Astro + TS + Tailwind)
- [x] 多存储适配器 (Local, R2, GitHub)
- [x] 环境变量配置 (.env)

### 图片处理流水线
- [x] 格式转换 (HEIC/TIFF -> JPEG)
- [x] **单张缩略图生成** (800w，简化自多尺寸)
- [x] EXIF 元数据提取 (相机, GPS, 拍摄参数)
- [x] ThumbHash 占位符生成（替代 BlurHash，体积更小质量更高）
- [x] 基于目录结构的自动标签生成
- [x] 构建产物生成 (`public/photos.json`)
- [x] **R2 全托管模式** - 缩略图上传到 R2

### 前端页面
- [x] 基础布局组件 (Header, Footer)
- [x] 博客功能 (基于 Astro Content Collections)
- [x] 画廊数据源接入

## 📝 待办事项 (Roadmap)

### 核心展示层
- [x] **响应式瀑布流画廊**
    - *状态*: 进行中 (见 `src/components/MasonicGallery.tsx`)
    - *目标*: 实现高性能、自适应的图片布局。
- [ ] **图片详情/全屏查看器**
    - *计划*: 集成 PhotoSwipe 或自研。
    - *功能*: 手势缩放、EXIF 信息展示面板、原图下载。
- [ ] **地图视图**
    - *计划*: 利用 EXIF GPS 数据在地图上展示照片。

### 性能优化
- [ ] WebGL 渲染器 (可选，视性能需求而定)
- [ ] 虚拟滚动 (已在 Masonic 中部分涉及)

### 其他
- [ ] 图片分享功能
- [ ] RSS 订阅优化

## 🔄 变更日志 (Change Log)

### 2026-03-12
- **修复 Node 脚本中 `import.meta.env` 为 `undefined` 的问题**:
  - **根因**: tsx 直接执行脚本时处于纯 Node.js 运行时，Vite/Astro 不会注入 `import.meta.env`；同时 Node 默认也不加载 `.env` 文件，导致 `process.env` 兜底同样为空。
  - **解决方案**:
    1. 安装 `dotenv`（devDependencies），在脚本入口添加 `import 'dotenv/config'`，确保 `.env` 在脚本启动时自动加载。
    2. `getEnv()` 保持双重兜底逻辑（优先 `import.meta.env`，再 `process.env`），兼容 Astro 页面和 Node 脚本两种运行时。
    3. 移除调试用 `console.log` 语句，清理代码噪音。
  - **受影响文件**:
    - `scripts/build-images-r2.ts` - 添加 `import 'dotenv/config'`
    - `src/lib/storage/index.ts` - 移除调试日志
    - `package.json` - 添加 `dotenv` devDependency

### 2026-02-11
- **参考 Afilmory 项目全面改进图片处理流水线**:

  1. **R2 S3 兼容配置**: R2Storage 添加 `requestChecksumCalculation` 和 `responseChecksumValidation` 配置，解决 AWS SDK v3 与 Cloudflare R2 的兼容性问题
  2. **EXIF 自动旋转**: `sharp.rotate()` 自动根据 EXIF Orientation 旋转图片，`getImageDimensions()` 考虑旋转后的实际宽高
  3. **BlurHash → ThumbHash**: 替换 `blurhash` 库为 `thumbhash`，体积更小、质量更高、支持透明度。数据字段从 `blurhash` 改为 `thumbHash`（Base64 编码）
  4. **JPEG → WebP 缩略图**: 默认缩略图格式从 JPEG 改为 WebP，更高效的压缩和更好的质量

- **受影响文件**:
  - `src/lib/storage/r2.ts` - 添加 R2 兼容配置
  - `src/lib/image/processor.ts` - 重写：rotate()、ThumbHash、WebP 默认
  - `src/lib/image/types.ts` - `blurhash` → `thumbHash`，移除 `blurhashComponents`
  - `src/lib/image/index.ts` - 适配新 API
  - `src/lib/image/index-r2.ts` - 适配新 API
  - `src/components/MasonicGallery.tsx` - 前端解码改用 `thumbHashToRGBA`
  - `src/components/BlurHashImage.astro` - 前端解码改用 `thumbHashToRGBA`
  - `src/pages/Gallery/[...slug].astro` - 修正数据字段引用
  - `scripts/build-images.ts` - WebP 格式，移除 blurhashComponents
  - `scripts/build-images-r2.ts` - WebP 格式，移除 blurhashComponents
  - `package.json` - 移除 `blurhash`，添加 `thumbhash`

### 2026-02-10
- **R2 全托管模式实现**: 
  - 创建 `src/lib/image/index-r2.ts` - R2 专用图片处理模块
  - 创建 `scripts/build-images-r2.ts` - R2 专用构建脚本
  - 添加 `pnpm run process-images:r2` 和 `pnpm run build:r2` 命令
  - 重写 `图片处理方案对比.md` - 完整的方案对比文档
  
- **R2 存储架构**:
  ```
  R2 Bucket/
  ├── photos/       # 原图（用户上传）
  └── .thumbnails/   # 缩略图（构建时生成上传）
  
  本地/
  └── public/photos.json  # 仅此一个文件
  ```
  
- **优势**:
  - 本地只保存 photos.json，仓库保持轻量
  - 充分利用 R2 存储和 Cloudflare CDN
  - 图片与代码完全分离
### 2025-11-27
- **初始化**: 创建 `开发日志.md`。
- **状态确认**: 确认项目基础设施及图片处理脚本已就绪。
- **文件核对**: 发现 `src/components/MasonicGallery.tsx`，推测瀑布流功能正在开发中。

---
*Agent 提示: 在添加新功能或修改现有逻辑后，请更新此文档的“变更日志”及“待办事项”状态。*
